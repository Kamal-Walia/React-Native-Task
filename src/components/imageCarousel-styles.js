import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  img: {
    height: '100%',
    width: '100%',
  },
  imgContainer: {
    paddingHorizontal: 8,
    height: 120,
    width: 185,
  },
});
export default styles;
